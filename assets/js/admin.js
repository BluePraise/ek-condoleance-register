/**
 * Condoleance Register - Admin JavaScript
 *
 * @package CondoleanceRegister
 * @since 2.0.0
 */

(function($) {
    'use strict';

    const CondoleanceAdmin = {
        init() {
            this.bindEvents();
        },

        bindEvents() {
            // Add admin-specific event handlers here
        }
    };

    $(document).ready(() => CondoleanceAdmin.init());

})(jQuery);
